# آخر خطوة يدوية لتفعيل GitHub Pages

من GitHub افتح المستودع `oroodi` ثم:

Settings → Pages → Source: Deploy from a branch → Branch: `main` → Folder: `/root` → Save.

بعدها تأكد أن Custom domain هو:

`oroodi.sa`
